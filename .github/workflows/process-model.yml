name: Process model

# One-off. Downloads the embedded-textures source model from the v0.1-assets
# release, compresses it with gltfpack, and uploads the result back to the
# same release under the name `npm run assets` expects.
#
# Run it from the Actions tab: Actions -> Process model -> Run workflow.
#
# Manual-only on purpose. It overwrites a release asset that every APK build
# depends on, so it should never fire on a push.

on:
  workflow_dispatch:
    inputs:
      source_asset:
        description: "Source asset filename on the v0.1-assets release."
        required: false
        default: "ImageToStl.com_OvalOffice_EmbededTextures.glb"
        type: string
      output_asset:
        description: "Name to publish the result under. fetch-assets.mjs hardcodes OvalOffice.glb."
        required: false
        default: "OvalOffice.glb"
        type: string
      texture_quality:
        description: "gltfpack -tc quality, 1-10. Lower is smaller."
        required: false
        default: "8"
        type: string
      simplify_ratio:
        description: "gltfpack -si ratio, 0-1. 1 disables simplification."
        required: false
        default: "0.5"
        type: string

permissions:
  contents: write

concurrency:
  group: process-model
  cancel-in-progress: false

jobs:
  process:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Download the source model
        env:
          SOURCE_ASSET: ${{ inputs.source_asset }}
        run: |
          set -euo pipefail
          url="https://github.com/joshworley21-rgb/Game-idea/releases/download/v0.1-assets/${SOURCE_ASSET}"
          echo "Downloading $url"
          curl -fL --retry 3 --retry-delay 5 -o raw_model.glb "$url"

          test -s raw_model.glb || {
            echo "::error::raw_model.glb is empty — the download was truncated."
            exit 1
          }
          ls -lh raw_model.glb

      # gltfpack ships as a prebuilt binary per platform. The npm package
      # `gltfpack` is a thin wrapper that fetches the same binary, but it has
      # no arm64 Linux build and its postinstall is unreliable on runners, so
      # the release tarball is the dependable path.
      - name: Install gltfpack
        run: |
          set -euo pipefail
          curl -fL --retry 3 -o gltfpack.tar.gz \
            "https://github.com/zeux/meshoptimizer/releases/download/v0.22/gltfpack-linux.zip"
          unzip -o gltfpack.tar.gz
          chmod +x gltfpack
          ./gltfpack -h 2>&1 | head -5 || true
          echo "$PWD" >> "$GITHUB_PATH"

      - name: Compress and optimise
        env:
          TEXTURE_QUALITY: ${{ inputs.texture_quality }}
          SIMPLIFY_RATIO: ${{ inputs.simplify_ratio }}
        run: |
          set -euo pipefail

          # -tc  compress textures (WebP/ktx2 depending on build)
          # -si  simplify to a ratio of the original triangle count
          # -cc  quantize attributes; three.js reads KHR_mesh_quantization
          #      natively, so no decoder ships with the game
          gltfpack \
            -i raw_model.glb \
            -o OvalOffice.glb \
            -tc \
            -si "$SIMPLIFY_RATIO" \
            -cc

          test -s OvalOffice.glb || {
            echo "::error::gltfpack produced no output."
            exit 1
          }

          raw=$(stat -c%s raw_model.glb)
          out=$(stat -c%s OvalOffice.glb)
          echo "raw:    $((raw / 1000000)) MB"
          echo "output: $((out / 1000000)) MB"
          echo "size_mb=$((out / 1000000))" >> "$GITHUB_OUTPUT"
        id: pack

      - name: Verify the result loads
        run: |
          set -euo pipefail
          npm install --no-save @gltf-transform/core @gltf-transform/extensions

          node --input-type=module -e '
          import { NodeIO } from "@gltf-transform/core";
          import { ALL_EXTENSIONS } from "@gltf-transform/extensions";

          const io = new NodeIO().registerExtensions(ALL_EXTENSIONS);
          const doc = await io.read("OvalOffice.glb");
          const root = doc.getRoot();

          const meshes = root.listMeshes();
          const textures = root.listTextures();
          const materials = root.listMaterials();

          let tris = 0;
          for (const mesh of meshes) {
            for (const prim of mesh.listPrimitives()) {
              const indices = prim.getIndices();
              const position = prim.getAttribute("POSITION");
              tris += indices ? indices.getCount() / 3 : (position?.getCount() ?? 0) / 3;
            }
          }

          console.log(`meshes:    ${meshes.length}`);
          console.log(`materials: ${materials.length}`);
          console.log(`textures:  ${textures.length}`);
          console.log(`triangles: ${Math.round(tris).toLocaleString()}`);

          // A GLB with no meshes is a broken export, not a small one.
          if (meshes.length === 0) {
            console.error("::error::No meshes in the output — the export is empty.");
            process.exit(1);
          }
          if (textures.length === 0) {
            console.warn("::warning::No textures in the output. The source had embedded textures, so they were lost.");
          }
          '

      - name: Publish to the v0.1-assets release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          OUTPUT_ASSET: ${{ inputs.output_asset }}
        run: |
          set -euo pipefail
          cp OvalOffice.glb "$OUTPUT_ASSET"

          # --clobber replaces the existing asset in place. Without it the
          # upload fails on a name collision, which is the common case here.
          gh release upload v0.1-assets "$OUTPUT_ASSET" --clobber

          echo "Published $OUTPUT_ASSET (${{ steps.pack.outputs.size_mb }} MB)"
          gh release view v0.1-assets --json assets \
            --jq '.assets[] | "  \(.name)  \(.size / 1000000 | floor) MB"'

      - name: Upload as a workflow artifact too
        uses: actions/upload-artifact@v4
        with:
          name: processed-model
          path: OvalOffice.glb
          if-no-files-found: error
